spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ckarena?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: ckarena
    password: ckarena
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080
